package com.cookcut.cookcut

import io.flutter.app.FlutterApplication

class CookCutApplication : FlutterApplication() {
    override fun onCreate() {
        super.onCreate()
        // Firebase is initialized by the Flutter Firebase plugin
    }
} 